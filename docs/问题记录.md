1、cli命令执行后，输出到聊天框的是一个提示框，并非嵌到AI交互数据流中，且这个提示框不会关闭
2、cli执行异常后，由于`SdtRunner` 是单例，一个时刻只能运行一个 testflow 进程，
就会报错："Another testflow process is already running"。
只能重启整个testagent，才能恢复
3、CLI test命令找不到，command test: not found